# [Created by task 2.4.1 3/20/2015 16:35:39]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=~/.task

# Holidays (see /usr/share/task/rc)
include /usr/share/task/rc/holidays.fr-FR.rc
# Color theme (see /usr/share/task/rc)
include /usr/share/task/rc/solarized-dark-256.theme

# Report settings
report.next.columns=id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.countdown,due.remaining,until.remaining,description.count,urgency      # task next: use the 'count' format (only hint number of annotations) for the 'description' column (other columns are default)

# Misc settings
regex=on            # handle regexps
journal.time=on     # let tasks be timed

