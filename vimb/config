# ~/.vimbrc - user vimb configuration file

set home-page=http://www.startpage.com

set editor-command=gvim -f %s

# reject third-party cookies
set cookie-accept=origin
# make cookies session-only
set cookie-expire-time=0
